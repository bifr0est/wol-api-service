services:
  wol-api:
    build: .
    container_name: wol-api-service
    network_mode: "host"  # Required for broadcasting WOL packets
    restart: unless-stopped
    environment:
      - PYTHONUNBUFFERED=1
      - PORT=9200
